---
import { pains } from "../../../data/pains";
import styles from "./ProblemSection.module.scss";
---

<section class={styles.section}>
  <div class={styles.container}>
    <div class:list={[styles.heading, "reveal"]}>
      <div class={styles.metaLine}>
        <span class={styles.metaBadge}>Seção 02</span>
        <span class={styles.metaDate}>O problema</span>
      </div>
      <h2>Se o plano não encaixa na vida real, você desiste antes do resultado.</h2>
    </div>

    <div class={styles.grid}>
      {pains.map((pain) => (
        <article class:list={[styles.card, "reveal"]}>
          <span class={styles.number}>{pain.number}</span>
          <h3>{pain.title}</h3>
          <p>{pain.text}</p>
        </article>
      ))}
    </div>
  </div>
</section>
